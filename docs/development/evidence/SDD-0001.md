# SDD-0001 validation evidence

Date: 2026-09-13. Reviewer/operator: Codex. Scope: repository development tooling only.

## Environment and identity

- Local Windows checkout; Python 3.12 from the Codex bundled runtime; GNU Bash 5.2.37 exposed as bundled `sh.exe`; local Git.
- Pre-governance repository baseline: `dd4b0f68ba4da15391bb8b8e1dcfdf2ae70a0746`.
- The final LF-normalized source/test fingerprints are recorded in [SDD-0001.json](../../changes/SDD-0001.json). This document does not identify a receiver APK, phone, IHU or vehicle test.
- PyYAML 6.0.3 was installed only in ignored `.test-output/skill-validator` for the existing skill validator and local workflow parsing; the SDD guard and unit suite use the Python standard library.

## G01–G10: automated behavior

Specification/test cases preceded implementation in [SDD-0001.md](../../changes/SDD-0001.md). The initial test-first run failed because the guard module did not yet exist. A later focused regression test for a changed automated-test file also failed before the fingerprint rule was corrected; it now passes. No retroactive claim of a hardware test is made.

| Check actually run | Result |
|---|---|
| `python -m unittest discover -s tests -v` with the bundled interpreter | PASS: 30 tests; 27 new SDD cases plus 3 existing profile cases, no skips; final direct run 35.862 s |
| Local `git hook run pre-commit` after selecting the bundled interpreter through local `sdd.python` | PASS: content guard, staged SDD guard and all 30 tests; suite 20.167 s |
| `python tools/repo_guard.py` | PASS |
| `python tools/sdd_guard.py` | PASS with reviewed ready SDD-0001 coverage |
| `python tools/sdd_guard.py --record docs/changes/SDD-0001.json` | PASS for scoped readiness |
| `python tools/sdd_guard.py --complete --record docs/changes/SDD-0001.json` before recording results | Expected FAIL: ready status, planned results and absent fingerprints correctly rejected |
| Workflow YAML parse and syntax checks for extracted CI script, shell helpers and hook | PASS |
| Local execution of the CI SDD shell step with pull-request, all-zero push and manual-dispatch event inputs | PASS: exit 0 for all three after correcting the manual baseline rule |
| `git diff --check` | PASS |

Behavioral tests use isolated temporary Git repositories and exercise undocumented-code rejection, positive ready/complete records, missing specs/review/test mappings, changed source/tests, missing/failed/blocked evidence, path traversal and staged symlink rejection, malformed field types, deletions/renames, old-record reuse, multi-commit base comparison, invalid bases, CLI exits, staged-versus-working-tree evidence, line-ending normalization and clean/dirty submodule fingerprints. Record-supplied test procedures are not executed.

The local hook invocation had no newly staged implementation in the owner checkout; staged rejection/acceptance is established by the temporary-repository behavioral cases. The hosted GitHub workflow was not run or published. The local CI-step execution validates the checked-in selection logic, not remote branch-protection configuration.

## G11: skill and guidance review

This was an author source walkthrough, not an independent subagent evaluation.

| Scenario | Reviewed outcome |
|---|---|
| A brief request to fix USB disconnects without mentioning specifications | AGENTS and skill require the affected design and cases before implementation; urgency does not silently waive SDD |
| Someone assumes AACP means CarPlay is already supported | Skill/context preserve wired Android Auto first and require a distinct scoped CarPlay receiver, licensing, integration and test design |
| A successful DHU demonstration is offered as v333 proof | Guidance distinguishes the phone baseline from the proposed APK and matching hardware |
| Host tests pass and someone proposes declaring the entire system complete | Guidance permits tooling/software-scoped completion only and requires all mandatory requirements plus G1–G5 for the vehicle system |
| Owner explicitly revises scope or process | User direction remains authoritative; document its impact and revise affected specifications rather than treating local guidance as higher authority |

The skill-creator `quick_validate.py` returned **Skill is valid!** with UTF-8 mode and its YAML dependency available. All six guidance/specification documents present at the link-check stage had valid local file links. The new evidence link is checked again during final validation.

## Completion boundary

After these results, set SDD-0001 to complete, attach this evidence to its cases, and capture the final source/test fingerprints. Run both default and completion guards on that record. Their success closes **repository governance tooling**, not the S70 Android Auto/CarPlay product.

The existing `docs/specs/REQUIREMENTS.md` was preserved. No new vehicle knowledge was merged into it, and no receiver build, hardware profile, APK installation, firmware modification, bench qualification or vehicle test was performed. The current lack of verified access, matching bench evidence and independent recovery remains a system blocker.
