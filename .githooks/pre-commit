#!/bin/sh
set -eu
sdd_python="${PYTHON:-$(git config --get sdd.python || true)}"
if [ -z "$sdd_python" ]; then
  sdd_python=python3
fi
"$sdd_python" tools/repo_guard.py
"$sdd_python" tools/sdd_guard.py --staged
"$sdd_python" -m unittest discover -s tests -q
