# Third-party source

`open-headunit` is a Git submodule pinned by `config/upstream.env`.

Initialize it with:

```bash
git submodule update --init --recursive
```

Do not replace the submodule with an untracked copy. Preserve its AGPL-3.0 licence and upstream history.
