# Research workspace

Keep only sanitized notes and original source code here. Raw profiles, logs, APKs and firmware belong under ignored local directories such as `artifacts/` and must not be committed.
